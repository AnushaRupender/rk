import { NotificationRecord } from './notification-record';

describe('NotificationRecord', () => {
  it('should create an instance', () => {
    expect(new NotificationRecord()).toBeTruthy();
  });
});
